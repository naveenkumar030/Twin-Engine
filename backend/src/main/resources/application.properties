server.port=${PORT:8080}
spring.application.name=fintwin-backend
spring.data.mongodb.uri=${MONGODB_URI:mongodb://localhost:27017/fintwin}
spring.data.mongodb.auto-index-creation=true

# Fail fast when MongoDB is unreachable (3s instead of default 30s)
spring.data.mongodb.server-selection-timeout=3000
